DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST:5432/DB_NAME?schema=public"

AUTH_SECRET="replace-with-a-long-random-secret"
AUTH_TRUST_HOST="true"
NEXT_PUBLIC_APP_URL="https://rpicommunity.vercel.app"
# Optional: only set this if the app is intentionally hosted under a subpath.
# NEXT_PUBLIC_APP_BASE_PATH="/rpicommunity"
UPLOAD_ROOT="./storage/private"
RESEND_API_KEY=""
RESET_EMAIL_FROM="RPIC Community <no-reply@example.com>"
# On Railway production, point this to a mounted persistent volume path, for example:
# UPLOAD_ROOT="/data/private"
# For development, you can use a local directory:
